const Manager = require('../lib/Manager');

test('creates an manager object', () => {
    const manager = new Manager('Roger', 25, 'roger7c@yahoo.com', '1300');
    
    expect(manager.officeNumber).toEqual(expect.any(Number));
});

officeNumber

getRole();
